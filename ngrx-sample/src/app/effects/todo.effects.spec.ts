import { TodoEffects } from './todo.effects';

describe('TodoEffects', () => {
  it('should create an instance', () => {
    //const directive = new TodoEffects();
    expect(directive).toBeTruthy();
  });
});
